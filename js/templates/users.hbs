<div class="row">
  <div class="col-md-4">
    <nav class="list-group">
      {{#each}}
        <a>{{displayName}}</a>
      {{/each}}
    </nav>
  </div>
  <div class="col-md-8">
    {{outlet}}
  </div>
</div>
